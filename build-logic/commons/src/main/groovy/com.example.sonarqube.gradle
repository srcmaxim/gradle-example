plugins {
    id('org.sonarqube')
}

sonarqube {
    properties {
        property 'sonar.projectKey', 'srcmaxim-gradle-example'
        property 'sonar.organization', 'srcmaxim'
        property 'sonar.host.url', 'https://sonarcloud.io'
    }
}
